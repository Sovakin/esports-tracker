# 🎮 Генератор сайта киберспортивных матчей

Генератор статического сайта, который по одному клику создаёт 3 HTML-страницы с информацией о киберспортивных матчах за вчера, сегодня и завтра.

## Возможности

- ✅ **3 страницы** — вчера (`yesterday.html`), сегодня (`index.html`), завтра (`tomorrow.html`)
- ✅ **Отдельные URL** — каждая страница имеет свой файл, без query-параметров
- ✅ **SEO-поля** — `<title>`, `<meta description>`, Open Graph, Twitter Card, canonical URL
- ✅ **Schema.org микроразметка** — JSON-LD (`Organization`, `WebPage`, `SportsEvent`, `SportsTeam`)
- ✅ **Дизайн на чистом HTML/CSS** — темная тема, анимации, адаптивность
- ✅ **Автоматическая генерация** — запуск одной командой

## Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Получение API-ключа

Зарегистрируйтесь на [PandaScore](https://pandascore.co/) и получите бесплатный API-ключ.

### 3. Запуск генератора

**Windows:**
```bash
set PANDASCORE_TOKEN=ваш_api_ключ
python generate.py
```

**Linux/Mac:**
```bash
export PANDASCORE_TOKEN=ваш_api_ключ
python generate.py
```

### 4. Результат

Сгенерированные файлы будут в папке `build/`:

```
build/
├── index.html        # Матчи за сегодня
├── yesterday.html    # Матчи за вчера
└── tomorrow.html     # Матчи за завтра
```

Откройте `build/index.html` в браузере.

## Демо-режим

Если API-ключ не задан, генератор создаст сайт с демонстрационными данными — для проверки дизайна и функциональности.

## Используемые API эндпоинты

| Эндпоинт | Назначение |
|---|---|
| `GET /matches` | Текущие матчи |
| `GET /matches/past` | Прошедшие матчи |
| `GET /matches/upcoming` | Предстоящие матчи |

## Структура проекта

```
site/
├── generate.py          # Генератор сайта
├── requirements.txt     # Зависимости Python
├── README.md           # Документация
└── build/              # Сгенерированный сайт
    ├── index.html
    ├── yesterday.html
    └── tomorrow.html
```

## Технические детали

- **Язык:** Python 3.8+
- **Зависимости:** только `requests`
- **Выходной формат:** статические HTML-файлы
- **Шрифт:** Inter (Google Fonts)
- **Дизайн:** тёмная тема с glassmorphism-эффектами
